import { ExerciseCardPreview } from 'my-v0-project'

// A self-contained design study of the set-logging card — it takes no props
// and renders its own current/proposed/compact comparison.
export function Default() {
  return <ExerciseCardPreview />
}
